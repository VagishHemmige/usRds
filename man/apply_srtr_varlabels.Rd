% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_srtr_varlabels.R
\name{apply_srtr_varlabels}
\alias{apply_srtr_varlabels}
\title{Apply SRTR variable labels to a data frame}
\usage{
apply_srtr_varlabels(df, dataset_key, verbose = FALSE)
}
\arguments{
\item{df}{A data frame from a SAF file (e.g., CAND_KIPA, TX_KI).}

\item{dataset_key}{Character. The dataset name used in the SRTR dictionary (e.g., "CAND_KIPA").}

\item{verbose}{Logical. If TRUE, prints each variable being labeled.}
}
\value{
A data frame with variable labels added (invisible via print, viewable via \code{labelled::var_label()}).
}
\description{
Adds descriptive variable labels to a dataset using the SRTR SAF data dictionary.
}
\examples{
\dontrun{
df <- read.csv("CAND_KIPA.parquet")
df <- apply_srtr_varlabels(df, dataset_key = "CAND_KIPA", verbose = TRUE)
labelled::var_label(df$ABO)
}
}
